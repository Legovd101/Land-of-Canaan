country_decisions = {
	anglosaxon_nation = {
		major = yes
		potential = {
			normal_or_historical_nations = yes
			NOT = { has_country_flag = formed_anglo_saxony_flag }
			was_never_end_game_tag_trigger = yes
			OR = {
				ai = no
				is_playing_custom_nation = no
			}
			OR = {
				is_free_or_tributary_trigger = yes
				ai = no
			}
			NOT = { exists = NGS }
			OR = {
				culture = anglosaxon
				culture = saxon
			}
			OR = {
				ai = no
				AND = {
					ai = yes
					num_of_cities = 6
				}
			}
			is_colonial_nation = no
			OR = {
				is_former_colonial_nation = no
				AND = {
					is_former_colonial_nation = yes
					ai = no
				}
			}
		}
		provinces_to_highlight = {
			OR = {
				culture = anglosaxon
				culture = saxon
			}
			OR = {
				NOT = { country_or_non_sovereign_subject_holds = ROOT }
				NOT = {	is_core = ROOT }
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			OR = {
				owns_all_provinces = {
					OR = {
						culture = anglosaxon
						culture = saxon
					}
				}
				AND = {
					tag = ENG
					SAX = { is_subject_of = ENG }
				}
				AND = {
					tag = SAX
					ENG = { is_subject_of = SAX }
				}
			}
		}
		effect = {
			every_subject_country = {
				limit = {
					OR = {
						tag = ENG
						tag = SAX
					}
				}
				ROOT = { inherit = PREV }
			}
			change_tag = NGS
			if = {
				limit = { culture = anglosaxon }
				override_country_name = NGS_anglish
			}
			if = {
				limit = { culture = saxon }
				override_country_name = NGS_saxon
			}
			if = {
				limit = {
					NOT = { government_rank = 2 }
				}
				set_government_rank = 2
			}
			france_region = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = NGS
			}
			home_counties_area = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = NGS
			}
			wessex_area = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = NGS
			}
			add_prestige = 25
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
			set_country_flag = formed_anglo_saxony_flag
		# if = {
		# 		limit = { 
		# 			has_custom_ideas = no
		# 			NOT = { has_idea_group = NSE_ideas }
		# 		}
		# 		country_event = { id = ideagroups.1 } #Swap Ideas
		# 	}
		}
		ai_importance = 400
	}

}