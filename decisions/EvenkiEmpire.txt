country_decisions = {

	restore_evenki_empire = {
		major = yes
		potential = {
			normal_or_historical_nations = yes
			OR = { # to prevent countries that can't form a prerequisite nation from seeing this
				# tag = YUA
				# tag = GLH
				# tag = ILK
				# custom_trigger_tooltip = {
				# 	tooltip = IS_CHAGATAI
				# 	has_country_flag = chagatai_formed_flag
				# }
				has_reform = holy_horde_reform
				was_never_end_game_tag_trigger = yes
			}
			OR = {
				culture_group = evenks
				has_reform = holy_horde_reform
			}
			NOT = { has_country_flag = evenki_empire_flag }
			NOT = { tag = HLR }
			NOT = { tag = ROM }
			NOT = { tag = PAP }
			NOT = { exists = LMD }
		}
		provinces_to_highlight = {
			OR = {
				AND = {
					OR = {
						AND = {
							region = mongolia_region
							is_empty = no
						}
						AND = {
							region = manchuria_region
							is_empty = no
						}
						region = central_asia_region
						region = crimea_region
						province_id = 1816
					}
					NOT = { owned_by = ROOT }
				}
				AND = {
					OR = {
						# region = khorasan_region
						# region = persia_region
						province_id = 295
						province_id = 280
					}
					NOT = {
						country_or_non_sovereign_subject_holds = ROOT
					}
				}
			}
		}
		allow = {
			# if = {
			# 	limit = {
			# 		has_reform = holy_horde_reform
			# 	}
			# 	OR = {
			# 		tag = YUA
			# 		tag = GLH
			# 		tag = ILK
			# 		custom_trigger_tooltip = {
			# 			tooltip = IS_CHAGATAI
			# 			has_country_flag = chagatai_formed_flag
			# 		}
			# 		has_reform = holy_horde_reform
			# 	}
			# }
			# else = {
			# 	OR = {
			# 		tag = YUA
			# 		tag = GLH
			# 		tag = ILK
			# 		custom_trigger_tooltip = {
			# 			tooltip = IS_CHAGATAI
			# 			has_country_flag = chagatai_formed_flag
			# 		}
			# 	}
			# }
			custom_trigger_tooltip = {
				tooltip = mongolia_decision_tooltip
				mongolia_region = {
					type = all
					OR = {
						is_empty = yes
						owner = {
							OR = {
								tag = ROOT
								is_subject_of = ROOT
							}
						}
					}
				}
			}
			manchuria_region = {
				type = all
				OR = {
					is_empty = yes
					owner = {
						OR = {
							tag = ROOT
							is_subject_of = ROOT
						}
					}
				}
			}
			central_asia_region = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
					owner = {
						OR = {
							tag = ROOT
							is_subject_of = ROOT
						}
					}
			}
			crimea_region = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
					owner = {
						OR = {
							tag = ROOT
							is_subject_of = ROOT
						}
					}
			}
			1816 = { country_or_non_sovereign_subject_holds = ROOT } # beijing		
			# khorasan_region = {
			# 	type = all
			# 	country_or_non_sovereign_subject_holds = ROOT
			# }
			# persia_region = {
			# 	type = all
			# 	country_or_non_sovereign_subject_holds = ROOT
			# }
			295 = { country_or_non_sovereign_subject_holds = ROOT }
			280 = { country_or_non_sovereign_subject_holds = ROOT }
			is_at_war = no
			is_free_or_tributary_trigger = yes
		}
		effect = {
			hidden_effect = {
				set_global_flag = is_forming_the_evenki_empire
				1 = {
					province_event = {
						id = passive_effects_events.13
						days = 10
					}
				}
			}
			change_tag = LMD
			on_change_tag_effect = yes
			if = {
				limit = {
					NOT = {
						government_rank = 3
					}
				}
				set_government_rank = 3
			}
			if = {
				limit = {
					is_emperor_of_china = yes
				}
				dismantle_empire_of_china = yes
			}
			if = {
				limit = { 
					is_revolutionary = no
					NOT = { has_reform = holy_horde_reform }
				}
				change_government = tribal
				add_government_reform = great_mongol_state_reform
			}
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
			set_country_flag = evenki_empire_flag
		}
		ai_will_do = {
			factor = 0
		}
	}
}